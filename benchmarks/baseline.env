# Baseline timings for protobuf-ada-bench on reference host profile.
# Update intentionally when algorithmic changes are accepted.
ENCODE_SECONDS=0.040304
DECODE_SECONDS=0.223948
MAX_REGRESSION_PERCENT=35
RUNS=3
